# Opcional. Sem isso, o front-end já descobre sozinho o endereço do backend
# (usa o mesmo host que você digitou no navegador, na porta 3000).
# Só preencha se quiser forçar um endereço específico.
# Copie esse arquivo para ".env" pra usar.

# VITE_API_URL=http://localhost:3000
