PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=BookSwap

# Chave usada para assinar o token de login (JWT).
# Em produção, use um valor longo e aleatório (ex: gerado com `openssl rand -hex 32`).
JWT_SECRET=
