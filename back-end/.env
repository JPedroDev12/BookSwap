PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=joao08
DB_NAME=BookSwap

# Chave usada para assinar o token de login (JWT). Em produção, troque
# por um valor longo e aleatório só seu.
JWT_SECRET=bookswap_dev_secret
